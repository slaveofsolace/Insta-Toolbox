# 4.1.6 acceptance

## Repairs

- Ghost discovery: exclude the native HTML Notes strip as well as ARIA lists and carousels. Native inspection found that Notes were being selected as conversation rows; the generated-browser fixture now includes that exact structural distinction.
- Short conversations: find native message rows beyond four wrapper levels and retain the real scroll container even when its content fits without scrolling. The failing three-message fixture previously finished with zero removals; it now removes all three.
- Reaction cleanup: preserve message identity while the native Reactions popup hides the conversation. Resolve the signed-in account through that exact popup state and deduplicate nested profile links. Hidden reaction badges cannot become false removal evidence.
- Removal results: a closed confirmation does not make an edited message a successful Unsend. Retained slots still work when the original payload detaches or its exact logical message key disappears.
- Mutual Checker: retain the stronger ID-backed record when another page repeats its username without an ID. Two conflicting explicit IDs still fail clearly.
- Stable updates: include the Ghost, streaming-history and Presence repairs from 4.1.5, which had not reached the latest-release userscript channel.

## Automated checks

The source suite contains **1,044 tests**. Generated-userscript parity, archive checks and repository hygiene are included. All **45 overlay** and **11 PWA** screenshot states pass the existing thresholds; baseline images are unchanged.

The native-shaped browser matrix includes one-, two- and three-message conversations, seven wrapper levels, direct/group conversations, unavailable account labels, received-message preservation, delayed history, voice/reel content, virtualized rows, Stop, retries and unresolved outcomes.

Reaction acceptance opens an exact native-shaped Reactions popup that hides the app behind it, removes one owned reaction, preserves the message and other reactions, and tests cancellation. Ghost acceptance uses three synthetic conversations, two reusable renderer windows, delayed mounts and real Web Locks. These are fixture results, not authenticated destructive acceptance.

Release promotion requires the complete extension/userscript suite, background fixtures, Chrome pairing, dependency audit, Windows/macOS package checks and checksums from one successful main CI run. The release workflow promotes those artifacts without rebuilding.

## Native inspection

The previous installed build reproduced the story-open/verification failure. After installing the pinned 4.1.5 build, a story-only Presence session opened a story, used native Next, recorded two verified actions and completed. No likes, follows, request acceptance or DM mutation was enabled. The Presence adapter is unchanged in 4.1.6.

The supplied short-conversation target displayed no message rows during inspection. Therefore current authenticated short-chat Unsend and reaction removal remain unverified. Before claiming those capabilities verified, use disposable direct/group messages and an owned reaction on the exact installed release. No private message, account or thread identifiers are stored here.

Instagram can omit list records or stop loading history. Partial comparisons remain readable and downloadable with counts and uncertainty; a successful fixture cannot establish complete server responses or whole-inbox deletion.

One native background comparison completed without a hang and returned fewer accounts than the exact profile totals. It displayed the partial comparison and both download options. This inspection establishes the current incomplete-response behavior, not its server-side cause or a complete-list fix.
