# 4.0.1 development acceptance

This is an unreleased 4.0.1 source candidate, not a stable release. The
published 3.1.12 update channel has not changed.

## Restored Unsend

- Removed Fast from the runner, reservations, preferences, and both in-page UIs.
- Restored visible-first whole-conversation traversal, bounded hover/menu waits,
  pre-dispatch removal observers, and adaptive 1–2 second pacing.
- Retained finite Newest/Oldest ordering, received-message protection, verified
  removal accounting, and Stop settlement without retrying uncertain actions.
- Generated-userscript reply and story fixtures each remove the intended newest
  message once, retain received messages, and report one verified removal.
- Direct and group reel fixtures remove both sent reels. An uncertain fixture
  reports uncertainty without retrying. These are synthetic layouts, not live
  account results or a throughput benchmark.
- Corrected the native ID-less proof for a short non-scrollable conversation.
  A final message no longer needs two surviving neighbors. Exact remaining
  messages, list/pane continuity and stable removal are still required.
- Five generated-userscript primary-button fixtures cover a final direct-chat
  message, two group reels, two reels under an “Instagram user” header without
  a profile link or composer, retained received messages, and an unchanged
  message that must stay uncertain. These do not simulate or establish actual
  blocked/deactivated-account behavior on Instagram.

## Inbox cleanup

The userscript includes native discovery, exact-ID selection, confirmation,
serial same-tab execution, Pause, reviewed Resume, Skip, Stop, and bounded local job history.
The confirmation discloses that messages sent during execution may also be
removed. Discovery may mark conversations read and does not claim full inbox
coverage without terminal proof.

Navigation requires an exact route and a proven replacement message pane;
changing the URL alone is insufficient. Reused or unproven empty panes stop
before Unsend. This native compatibility gate remains open.

Behavioral fixtures cover cancellation, two selected conversations, account
changes, stale confirmation, busy account locks, interrupted storage, retained
verified counts, and reload without restored authority. Rendered userscript
checks cover dark/light, narrow, short-window, and real 200% zoom layouts with
44px controls and one live region. Screenshots contain synthetic content only.

Native header labels and a name/handle filter are connected to the review list.
Three rendered behavioral gates cover discovery labels, plain versus verified
handle matching, preserved hidden selections, and exact Review/Cancel without
removals. Five populated-list layout states cover the same viewport matrix.
An additional generated-userscript flow selects two synthetic chats, accepts the
exact confirmation and uses the production controller plus shared DM runner.
It removes one own message per selected chat in order, preserves four received
messages, leaves an unselected chat unchanged and stores no action authority.

## Presence

Presence is a compact userscript session rather than a plan builder. It exposes
five choices: view stories, react to stories, like posts, follow people, and
accept follow requests. The only other choice is a finite maximum from 1 to 50.
Manual Follow / Unfollow remains available in one secondary disclosure.

Start opens one review naming the signed-in account, enabled actions, and
maximum. Accepted authority exists only in memory, expires within 15 minutes,
and is bound to that account and exact choice set. Pause prevents the next
action, Stop aborts the wait before another action, and an uncertain result
halts the session without retrying it.

The native adapter re-resolves visible posts, profiles, requests, and stories
immediately before one click and requires a matching postcondition. Story
reactions include opening the story first. Seven focused controller tests and
seven generated-userscript browser gates cover runtime-only review authority,
Pause/Resume/Stop, exact target changes, confirmation-before-click, post likes,
follows, request acceptance, story opening/reaction, cancellation, and five
responsive states including true 200% zoom. These are deterministic fixtures,
not current authenticated Instagram compatibility.

## Verified checks

- Restoration checkpoint `b6d3521`: 869 passing tests. Integration checkpoint
  `011681c`: 908 passing tests; CI, CodeQL and dependency review all passed.
- Current source: 972 passing tests.
- Full extension/userscript acceptance, including finite ordering, virtualized
  traversal, Stop, partial Mutual Checker review, cooldowns, and confirmation.
- Seven inactive/frozen-page lifecycle fixtures, including verified and uncertain
  dispatched outcomes. No native background compatibility is inferred.
- Assembly, dependency verification, repository hygiene, generated-userscript
  parity, extension build checks, and static web archive inspection.
- The historical 45-state 4.0.0 overlay matrix retains its pixel thresholds.
  The 4.0.1 candidate still needs its final release-candidate visual recapture.

After reinstalling the commit-pinned `011681c` userscript, a read-only check in
the current Chrome session found the restored Unsend button, no Speed selector,
and the connected Inbox cleanup and Presence sections. No removal was attempted
during this installation check. That observation does not close the live Unsend,
reaction or multi-conversation acceptance gates.

## Remaining acceptance

- Source-matched authenticated Unsend after restoration.
- Native inbox discovery and same-tab transitions, including empty conversations
  and layouts that reuse their message pane.
- Disposable two-conversation cleanup with Stop and no unrelated changes.
- Own-reaction removal before enabling its existing follow-up adapter.
- Authenticated Presence acceptance against current Instagram layouts and
  shared Presence/Ghost account ownership.
- Managed worker tabs and authenticated background lifecycle support.
- Exact-commit CI, final visual recapture, package/release promotion, and
  Tampermonkey update verification after the test build is accepted.

No live mutation was performed for this checkpoint. Main, the stable release,
and existing account data remain unchanged.
